You are Synapse #732 Agent A.

Do not edit files. Do not run tests.

Use the Synapse MCP tool `control_lease_acquire` with `ttl_ms=30000` now.
Then run this shell command and wait for it to finish:

Start-Sleep -Seconds 28

Do not call `control_lease_release`.
Do not finish your response until the sleep command completes.
Return the acquire result and Synapse session id.
